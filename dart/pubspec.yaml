name: vscode_textmate
description: vscode-textmate
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  path: ^1.9.1
  wasm_run: ^0.1.0+2
  xml: ^6.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
